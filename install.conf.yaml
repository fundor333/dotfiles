- defaults:
    link:
      create: true
      relink: true
      force: true

- create:
    ~/Coding:
    ~/.gostuff:

- link:
    ~/.zshrc:
    ~/.irbrc:
    ~/.editorconfig:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.Xresources:
    ~/.ssh/config: ssh/config
    ~/script:
    ~/.emacs.d:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [chmod +x ~/script/*]
