- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.zshrc:
    ~/.irbrc:
    ~/.editorconfig:
    ~/.gitconfig:
    ~/script:
    ~/Xresources:
    ~/.ssh/config: ssh/config
    ~/.emacs.d:


- shell:
  - [git submodule update --init --recursive, Installing submodules]
